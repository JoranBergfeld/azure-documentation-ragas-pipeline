name: ragas-infused-pipeline
metadata:
  template: ragas-infused-pipeline@0.1.0
hooks:
  postprovision:
    shell: sh
    run: |
      uv run python -m ragpipe.ingest
      uv run python scripts/setup_agents.py
